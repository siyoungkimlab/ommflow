## What and why

<!-- What changes, and what problem it solves. This becomes the squash commit
     message on main, so write it for someone reading `git log` in a year. -->

## How it was verified

<!-- Commands run, systems tested, numbers measured. "Tests pass" is what CI
     is for; say what you checked that CI cannot. -->

## Notes for the reviewer

<!-- Trade-offs, anything deliberately left out, follow-up work. Delete if
     there is nothing to flag. -->

---

- [ ] `pytest tests` passes locally
- [ ] Documentation updated (README and `docs/`) if behavior or settings changed
- [ ] New settings appear in the config template, `final.toml`, and the CLI reference
